#!/usr/bin/env python3

import sys
import os
from pathlib import Path

BIN_DIR = Path(__file__).resolve().parent
MODULES_DIR = BIN_DIR / "modules"
VENV_PYTHON = BIN_DIR.parent / "opt" / "venv" / "bin" / "python"
QUEUE_SCRIPT = MODULES_DIR / "queue.py"

if not VENV_PYTHON.exists():
    sys.stderr.write("❌ Virtual environment not found. Please run bin/install first.\n")
    sys.exit(1)

args = [str(VENV_PYTHON), str(QUEUE_SCRIPT)] + sys.argv[1:]
os.execv(str(VENV_PYTHON), args)
